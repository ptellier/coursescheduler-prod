const checkFullRestricted = (sections) => {
    let restricted = []
    let available = []
    let full = []
    sections.forEach((section) => {
        if (section.status === 'Available') {
        }
        if (section.status === 'Blocked') {
        }
        if (section.status === 'Restricted') {
        }
        if (section.status === 'Full') {
        }
    })
}
